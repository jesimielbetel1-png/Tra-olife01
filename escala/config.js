// =====================================================================
//  CONFIGURAÇÃO — cole aqui as chaves do seu projeto Supabase
//  (Supabase -> Project Settings -> API)
//
//  Enquanto ficar em branco, o site roda em MODO DEMONSTRAÇÃO:
//  usa dados de exemplo salvos no próprio navegador (localStorage),
//  sem login por e-mail e sem servidor. Assim você testa tudo antes
//  de conectar o Supabase de verdade.
// =====================================================================
window.CONFIG = {
  // Cole a "Project URL"  (ex.: https://xxxxx.supabase.co)
  SUPABASE_URL: "",

  // Cole a "anon public key" (chave longa; pode ficar no site — é pública por design)
  SUPABASE_ANON_KEY: "",

  // Usuário do seu bot no Telegram, SEM o @ (ex.: EscalaAdvecBot)
  // Usado para montar o link de vínculo:  https://t.me/SEU_BOT?start=TOKEN
  TELEGRAM_BOT: "",

  // Número do WhatsApp da liderança (com DDI+DDD, só dígitos) para o botão
  // de "cobrar quem não lançou" via click-to-send. Ex.: 5581999998888
  WHATSAPP_LIDERANCA: "",
};
